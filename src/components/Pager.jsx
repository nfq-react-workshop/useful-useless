import * as React from 'react';

const Pager = () => (
    <nav>
        <a href="#prev">prev</a>
        <a href="#1">1</a>
        <a href="#1">2</a>
        <a href="#next">next</a>
    </nav>
);

export default Pager;
